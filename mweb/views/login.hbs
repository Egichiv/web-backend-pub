<!-- mweb/views/login.hbs -->
{{> header}}
<div class="page-container">
  <section class="general-section">
    <h1 class="page-title">Вход в систему</h1>

    {{#if errorMessage}}
      <div class="alert alert-error">
        ❌ {{errorMessage}}
      </div>
    {{/if}}

    {{#if successMessage}}
      <div class="alert alert-success">
        ✅ {{successMessage}}
      </div>
    {{/if}}

    <div class="form-container">
      <form action="/users/login" method="POST" class="login-form">
        <div class="form-group">
          <label for="loginUsername" class="required">Имя пользователя:</label>
          <input type="text"
                 id="loginUsername"
                 name="nickname"
                 required
                 placeholder="Введите ваше имя пользователя"
                 class="form-input">
        </div>

        <div class="form-group">
          <label for="loginPassword" class="required">Пароль:</label>
          <input type="password"
                 id="loginPassword"
                 name="password"
                 required
                 placeholder="Введите ваш пароль"
                 class="form-input">
        </div>

        <div class="form-actions">
          <button type="submit" class="btn btn-primary btn-large">
            Войти
          </button>
        </div>
      </form>

      <div class="auth-links">
        <p>
          Нет аккаунта?
          <a href="/register" class="btn btn-outline">Зарегистрироваться</a>
        </p>
      </div>

      <!-- Информация для тестирования -->
      <div class="alert alert-info">
        <strong>Для тестирования:</strong><br>
        Логин: <code>boban</code><br>
        Пароль: <code>admin</code>
      </div>
    </div>
  </section>

  <!-- Информационная секция -->
  <section class="general-section">
    <h2 class="section-title">Зачем нужна авторизация?</h2>
    <div class="info-content">
      <p>После авторизации вам станут доступны следующие возможности:</p>
      <ul class="features-list">
        <li>Добавление новых цитат без модерации</li>
        <li>Возможность оставлять комментарии от своего имени</li>
        <li>Управление своими цитатами и комментариями</li>
        <li>Загрузка мемов</li>
        <li>Создание постов</li>
        <li>Доступ к персональному профилю</li>
      </ul>
    </div>
  </section>
</div>