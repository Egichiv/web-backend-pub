<div class="container mt-5">
  <h1>Добавить мем</h1>

  <div class="row">
    <!-- Форма загрузки файла -->
    <div class="col-md-6">
      <h3>Загрузить файл</h3>
      <form
        id="uploadForm"
        action="/api/memes/upload"
        method="POST"
        enctype="multipart/form-data"
      >
        <div class="form-group">
          <label for="file">Выберите картинку:</label>
          <input
            type="file"
            class="form-control"
            id="file"
            name="file"
            accept="image/jpeg,image/png,image/gif,image/webp"
            required
          >
          <small class="form-text text-muted">
            Форматы: JPG, PNG, GIF, WEBP. Макс. размер: 5MB
          </small>
        </div>

        <div class="form-group">
          <label for="uploader">Ваше имя:</label>
          <input
            type="text"
            class="form-control"
            id="uploader"
            name="uploader"
            placeholder="anonymous"
          >
        </div>

        <button type="submit" class="btn btn-primary">
          📤 Загрузить файл
        </button>
      </form>
    </div>

    <!-- Форма добавления по ссылке -->
    <div class="col-md-6">
      <h3>Или добавить по ссылке</h3>
      <form action="/memes/create" method="POST">
        <div class="form-group">
          <label for="link">Ссылка на мем:</label>
          <input
            type="url"
            class="form-control"
            id="link"
            name="link"
            placeholder="https://i.imgur.com/example.jpg"
            required
          >
        </div>

        <div class="form-group">
          <label for="uploader2">Ваше имя:</label>
          <input
            type="text"
            class="form-control"
            id="uploader2"
            name="uploader"
            placeholder="anonymous"
          >
        </div>

        <button type="submit" class="btn btn-secondary">
          🔗 Добавить по ссылке
        </button>
      </form>
    </div>
  </div>

  <div class="mt-3">
    <a href="/memes" class="btn btn-outline-secondary">← Назад к мемам</a>
  </div>
</div>

<script>
  // Обработка загрузки файла через AJAX
  document.getElementById('uploadForm').addEventListener('submit', async (e) => {
    e.preventDefault();

    const formData = new FormData(e.target);
    const submitBtn = e.target.querySelector('button[type="submit"]');
    submitBtn.disabled = true;
    submitBtn.textContent = '⏳ Загрузка...';

    try {
      const response = await fetch('/api/memes/upload', {
        method: 'POST',
        body: formData,
      });

      const data = await response.json();

      if (response.ok) {
        window.location.href = '/memes?success=meme_created';
      } else {
        alert('Ошибка: ' + (data.message || 'Не удалось загрузить мем'));
        submitBtn.disabled = false;
        submitBtn.textContent = '📤 Загрузить файл';
      }
    } catch (error) {
      alert('Ошибка сети: ' + error.message);
      submitBtn.disabled = false;
      submitBtn.textContent = '📤 Загрузить файл';
    }
  });
</script>